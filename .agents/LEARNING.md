- [TEST] Portfolio unit tests (vitest via @angular/build:unit-test): running under system Node 25 breaks all specs using localStorage ("localStorage.clear is not a function") because Node 25 ships a built-in webstorage that shadows jsdom's. Always run with Node 24 from .nvmrc; on this machine `mise x node@24 --` does not win over Homebrew node in PATH — export PATH="$(mise where node@24.19.0)/bin:$PATH" instead. | Impact: avoids misdiagnosing 17 phantom test failures as regressions.
- [ARCH] portfolio-page.component.scss cascade trap: `.impact` sets padding-inline, but any later media-query rule re-declaring `.section { padding: … }` silently zeroes it (same specificity, later source order) since the section carries both classes. | Impact: when adding responsive .section overrides, re-assert .impact padding-inline in the same block.
- [FAIL] The assistant panel header (minimize button) was removed from markup but e2e portfolio.spec.ts kept testing the old hide flow and the EN fallback gateway lost its career-path entry ('career path' term sat in the small-talk answer). Unit specs assert the header is gone, so unit and e2e specs contradicted each other. | Impact: after UX refactors here, sweep e2e + fallback-gateway terms for staleness, not just unit specs.
